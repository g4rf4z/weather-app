import { defineStore } from "pinia";
import { apiWrapper } from "@/services/api";

const useMapboxStore = defineStore({
  id: "mapboxStore",
  state: () => ({}),

  getters: {},

  actions: {},
});

export { useMapboxStore };
