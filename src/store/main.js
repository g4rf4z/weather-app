import { defineStore } from "pinia";
import { apiWrapper } from "@/services/api";

const store = defineStore({
  // Data
  state: () => ({}),
  // Get
  getters: {},
  // Method
  actions: {},
});

export { store };
